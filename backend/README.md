# arxgorithm backend

FastAPI-based API server.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## API Docs

Running locally: http://localhost:8000/docs (Swagger UI)
